namespace FamilyTaskManagerAPI.Entities
{
    public enum UserRole
    {
        User,
        Admin
    }

    public enum TaskItemStatus
    {
        Pending,
        InProgress,
        Completed,
        Overdue
    }
}
